'use client';

import type { LangFeatures } from '@/core/lib/types';

type Props = {
  features: LangFeatures[];
};

function LangFeaturesForm({ features }: Props) {
  return <div></div>;
}

export default LangFeaturesForm;
